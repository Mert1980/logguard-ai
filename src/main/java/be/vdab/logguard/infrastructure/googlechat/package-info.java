/**
 * Google Chat delivery: an alternative {@code TerminalOutputPort} that POSTs every notification to an
 * incoming webhook instead of {@code System.out}, active only when {@code logguard.google-chat.enabled=true}.
 */
package be.vdab.logguard.infrastructure.googlechat;
